export type Array2dTranspose = <T>(matrix: T[][]) => T[][];
/**
 * Bounding box object
 */
export type BBox = {
    /**
     * Start x coordinate
     */
    minX: number;
    /**
     * End x coordinate
     */
    maxX: number;
    /**
     * Start y coordinate
     */
    minY: number;
    /**
     * End y coordinate
     */
    maxY: number;
};
/**
 * Option object of `sortPos()`
 */
export type SortPosOptions = {
    /**
     * Value getter
     */
    getter: (v: any) => number;
    /**
     * Sort function
     */
    comparator: (a: number, b: number) => number;
    /**
     * If `true` ignore `null` values
     */
    ignoreNull: boolean;
};
/**
 * Method to add a class name to an HTML or SVG element.
 * @param {HTMLElement | SVGElement} element - HTML or SVG element to add a class to.
 * @param {string} className - The class name to be added.
 */
export function addClass(element: HTMLElement | SVGElement, className: string): void;
/**
 * Aggregate a vector using one or more aggregators. Like a multi-purpose reducer.
 * @param {number[]} v - Numerivcal vector
 * @param {function | function[]} aggregater - A single or multiple aggregator
 *   functions. The aggregator functions work like reducers.
 * @param {number | number[]} startValue - A single or multiple start values
 * @param {function} options.getter - A value getter
 * @return {number | number[]} A single or multiple aggregagted values
 */
export function aggregate(v: number[], aggregater: Function | Function[], startValue: number | number[], { getter }?: Function): number | number[];
/**
 * Return a list of indices sorted by the array
 *
 * @example
 * const X = [9, 5, 11, -1, 0];
 * const sortedIdx = argSort(X);
 * // >> [3, 4, 1, 0, 2]
 * // I.e., the smallest element is X[sortedIdx[0]] == -1
 *
 * @param {array} array - Array of numerical values
 * @param {function} comparator - Pairwise value comparator function
 * @return {array} Array of indices sorted by the values
 */
export function argSort(array: any[], { getter, comparator, ignoreNull }?: Function): any[];
/** @typedef {<T>(matrix: T[][]) => T[][]} Array2dTranspose */
/**
 * Transpose a nested 2D array
 * @type {Array2dTranspose}
 * @param {array} matrix - The matrix-liked 2D nested array to be transposed
 * @return {array} The transposed 2D nested matrix-like array
 */
export const array2dTranspose: Array2dTranspose;
/**
 * Assign properties, constructors, etc. to an object
 *
 * @param {object} target - The target object that gets `sources` assigned to it
 * @param {}
 * @return {object}
 */
export function assign(target: object, ...sources: any[]): object;
/**
 * Convert from camelCase to CONST_CASE
 * @example
 * ```js
 * camelToConst('myCamelCaseString') // => 'MY_CAMEL_CASE_STRING'
 * ```
 * @param {string} str - The camelCase string to be converted to CONST_CASE
 * @return {string} The resulting string in CONST_CASE
 */
export function camelToConst(str: string): string;
/**
 * Convert a string captical case
 * @example
 * ```js
 * capitalize('my string') // => 'My string'
 * ```
 * @param {string} str - The string to be converted
 * @return {string} The resulting string in capitcal case
 */
export function capitalize(str: string): string;
/**
 * Restrict value to be within [min, max]
 * @description About 18% faster than `Math.max(min, Math.min(max, value))`
 * @param {number} value - Value to be clamped
 * @param {number} min - Min value
 * @param {number} max - Max value
 * @return {number} Clamped value
 */
export function clamp(value: number, min: number, max: number): number;
/**
 * Clear an array without while keeping it's reference
 * @type {<T>(a: T[]) => T[]}
 * @param {array} a - Array to be cleared
 * @return {array} The array itself
 */
export const clearArray: <T>(a: T[]) => T[];
/**
 * Clone an event by invoking the source event's constructor and passing in
 *   the source event.
 *
 * @param {Event} event - Source event to be cloned.
 * @return {Event} Cloned event
 */
export function cloneEvent(event: Event): Event;
/**
 * Create HTML from a template string
 * @param {string} template - HTML template string
 * @return {Node} Root DOM element
 */
export function createHtmlByTemplate(template: string): Node;
/**
 * Create a worker from a function
 * @param {function} fn - Function to be turned into a worker
 * @return {Worker} Worker function
 */
export function createWorker(fn: Function): Worker;
/**
 * Cubic in easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function cubicIn(t: number): number;
/**
 * Cubic in and out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function cubicInOut(t: number): number;
/**
 * Cubic out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function cubicOut(t: number): number;
/**
 * Debounce a function call.
 *
 * @description
 * Function calls are delayed by `wait` milliseconds and only one out of
 * multiple function calls is executed.
 *
 * @param {function} fn - Function to be debounced
 * @param {number} wait - Number of milliseconds to debounce the function call.
 * @return {function} Debounced function
 */
export function debounce(fn: Function, wait: number): Function;
/**
 * Convert a decimal to its RGB representation
 * @param {number} dec - Decimal-encoded color.
 * @return {number[]} RGB array
 */
export function decToRgb(dec: number): number[];
/**
 * Deep clone an object.
 *
 * @param {object} source - Object to be cloned.
 * @return {object} Cloned `source` object.
 */
export function deepClone(source: object): object;
/**
 * Get the difference of two vectoe
 * @param {number[]} v - Numerical vectors
 * @param {number[]} w - Numerical vectors
 * @return {number[]} Difference vector
 */
export function diff(v: number[], w: number[]): number[];
/**
 * Extend an object with another object.
 *
 * @param {object} target - Target object or `undefined` if a new object should
 *   be created.
 * @param {object} source - Object to be cloned.
 * @return {object} Cloned `source` object
 */
export function extend(target: object, source: object): object;
/**
 * Functional version of `Array.forEach`
 *
 * @description
 * More flexible and applicable to other array-like data types like `NodeList`.
 *
 * @type {<I extends Iterable<T>>(f: (value: T) => void) => (x: I) => void}
 * @param {function} f - Callback function applied on every item of the array.
 * @return {array} Modified array-like variable.
 */
export const forEach: <I extends Iterable<T>>(f: (value: T) => void) => (x: I) => void;
/**
 * Forward an event by cloning and dispatching it.
 * @param {Event} event - Event to be forwarded.
 * @param {HTMLElement} target - Target HTML element for the event.
 */
export function forwardEvent(event: Event, target: HTMLElement): void;
/**
 * Check if an HTML or SVG element has a certain class
 * @param {HTMLElement | SVGElement} element - HTML or SVG element to be checked
 * @param {string} className - Class name to be checked for
 * @return {boolean} If `true` `element` has the class name
 */
export function hasClass(element: HTMLElement | SVGElement, className: string): boolean;
/**
 * Check if two arrays contain the same elements
 * @type {<T>(a: T[], b: T[]) => Boolean}
 * @param {array} a - First array
 * @param {array} b - Second array
 * @return {boolean} If `true` the two arrays contain the same elements
 */
export const hasSameElements: <T>(a: T[], b: T[]) => boolean;
/**
 * Convert a HEX string to its decimal representation
 * @param {string} hex - HEX string
 * @return {number} Decimal representation
 */
export function hexToDec(hex: string): number;
/**
 * Convert a HEX-encoded color to an RGB-encoded color
 * @param {string} hex - HEX-encoded color string.
 * @param {boolean} normalizeColor - If `true` the returned RGB values will be
 *   normalized to `[0,1]`.
 * @return {array} Triple holding the RGB values.
 */
export function hexToRgbArray(hex: string, normalizeColor?: boolean): any[];
/**
 * Convert a HEX-encoded color to an RGBA-encoded color
 * @param {string} hex - HEX-encoded color string.
 * @param {boolean} normalizeColor - If `true` the returned RGBA values will be
 *   normalized to `[0,1]`.
 * @return {array} Triple holding the RGBA values.
 */
export function hexToRgbaArray(hex: string, normalizeColor?: boolean): any[];
/**
 * Identity function
 * @type {<T>(x: T) => T}
 * @param {*} x - Any kind of value
 * @return {*} `x`
 */
export const identity: <T>(x: T) => T;
/**
 * Linearly interpolate two numbers
 * @param {number} a - The start value
 * @param {number} b - The end value
 * @param {number} p - The interpolation progress. Must be in [0, 1] where `0`
 *   refers to the start value and `1` to the end value
 * @return {number} The interpolated number
 */
export function interpolateNumber(a: number, b: number, p: number): number;
/**
 * Lineraly interpolate a numerical vector
 * @param {array} a - The start vector
 * @param {array} b - The end vector
 * @param {number} p - The interpolation progress. Must be in [0, 1] where `0`
 *   refers to the start vector and `1` to the end vector
 * @return {array} The interpolated vector
 */
export function interpolateVector(a: any[], b: any[], p: number): any[];
/**
 * Test if a variable is an array
 * @param {any} a - The variable to test
 * @return {Boolean} If `true` the variable is an array.
 * @type {(a: any) => a is any[]}
 */
export const isArray: (a: any) => a is any[];
/**
 * Test if two floats are close given some precision
 * @param {number} a - First float
 * @param {number} b - Second float
 * @param {number} precision - Number of decimal places to check
 * @return {boolean} If `true` the difference between the floats is less than
 *   10^-precision
 */
export function isClose(a: number, b: number, precision?: number): boolean;
/**
 * Test if a variable is a function
 * @param {*} f - The variable to test
 * @return {boolean} If `true` the variable is a function.
 * @type {(f: any) => f is Function}
 */
export const isFunction: (f: any) => f is Function;
/**
 * Tests if a string is a valid HEX color encoding
 * @param {string} hex - HEX-encoded color string.
 * @return {boolean} If `true` the string is a valid HEX color encoding.
 */
export function isHex(hex: string): boolean;
/**
 * Tests if a number is in `[0,1]`.
 * @param {number} x - Number to be tested.
 * @return {boolean} If `true` the number is in `[0,1]`.
 */
export function isNormFloat(x: number): boolean;
/**
 * Tests if an array consist of normalized numbers that are in `[0,1]` only.
 * @param {*} a - Array to be tested
 * @return {boolean} If `true` the array contains only numbers in `[0,1]`.
 */
export function isNormFloatArray(a: any): boolean;
/**
 * Test if a variable is a number
 * @param {*} x - Variable to be tested
 * @return {boolean} If `true`, `x` is a number.
 * @type {(x: any) => x is number}
 */
export const isNumber: (x: any) => x is number;
/**
 * Test if a variable is a plain object, e.g., `{}`
 * @param {*} o - The variable to test
 * @return {boolean} If `true` the variable is a plain object.
 * @type {(o: any) => o is Object}
 */
export const isObject: (o: any) => o is any;
/**
 * Test whether a DOM element is the parent of another DOM element.
 *
 * @param {HTMLElement} element - Potential child element.
 * @param {object} parent - Target parent element which is tested to have `el`
 *   as a child.
 * @return {boolean} If `true` `parent` is a parent of `element`.
 */
export function isParentOf(element: HTMLElement, parent: object): boolean;
/**
 * Check if a 2D or 1D point is within a rectangle or range
 * @param {[number, number]} point - The point defined as a tuple of [x, y]
 *   coordinates.
 * @param {[number, number, number, number]} rectangle - The rectangle defined
 *   as a quadruple of [minX, maxX, minY, maxY] coordinates.
 * @return {Boolean} If `true` the [x,y] point is in the rectangle.
 */
export function isPointHalfwayInRect([x, y]: [number, number], [minX, maxX, minY, maxY]: [number, number, number, number]): boolean;
/**
 * From: https://wrf.ecse.rpi.edu//Research/Short_Notes/pnpoly.html
 * @param {[number, number]} point - The point defined as a tuple of [x, y]
 *   coordinates.
 * @param {number[]} polygon - 1D list of vertices defining the polygon.
 * @return {Boolean} If `true` point lies within the polygon.
 */
export function isPointInPolygon([px, py]?: [number, number], polygon?: number[]): boolean;
/**
 * Check if a 2D or 1D point is within a rectangle or range
 * @param {[number, number]} point - The point defined as a tuple of [x, y]
 *   coordinates.
 * @param {[number, number, number, number]} rectangle - The rectangle defined
 *   as a quadruple of [minX, maxX, minY, maxY] coordinates.
 * @return {Boolean} If `true` the [x,y] point is in the rectangle.
 */
export function isPointInRect([x, y]: [number, number], [minX, maxX, minY, maxY]: [number, number, number, number]): boolean;
/**
 * Tests if an array is encoding an RGB color.
 * @param {number[]} rgb - Array to be tested
 * @return {boolean} If `true` the array hold a triple of Uint8 numbers or
 *   a triple of normalized floats.
 */
export function isRgbArray(rgb: number[]): boolean;
/**
 * Tests if a string is encoding an RGB color.
 * @param {string} str - String to be tested
 * @return {boolean} If `true` the array hold a triple of Uint8 numbers or
 *   a triple of normalized floats.
 */
export function isRgbStr(str: string): boolean;
/**
 * Tests if an array is encoding an RGBA color.
 * @param {number[]} rgba - Array to be tested
 * @return {boolean} If `true` the array hold a quadruple of normalized floats,
 *   a quadruple of Uint8s, or a triple of Uint8 and one normalized float.
 */
export function isRgbaArray(rgba: number[]): boolean;
/**
 * Tests if a string is encoding an RGBA color.
 * @param {string} str - String to be tested
 * @return {boolean} If `true` the array hold a quadruple of Uint8 numbers or
 *   a quadruple of normalized floats.
 */
export function isRgbaStr(str: string): boolean;
/**
 * Tests if a variable is a string
 * @param {*} s - Variable to be tested
 * @return {boolean} If `true` variable is a string
 * @type {(s: any) => s is string}
 */
export const isString: (s: any) => s is string;
/**
 * Tests if a number is an interger and in `[0,255]`.
 * @param {number} x - Number to be tested.
 * @return {boolean} If `true` the number is an interger and in `[0,255]`.
 */
export function isUint8(x: number): boolean;
/**
 * Tests if an array consist of Uint8 numbers only.
 * @param {*} a - Array to be tested.
 * @return {boolean} If `true` the array contains only Uint8 numbers.
 * @type {(a: any): a is number[]}
 */
export const isUint8Array: (a: any) => any;
/**
 * Store the values of an iterator in an array.
 *
 * @description This code is about 9x faster than `Array.from()`.
 *
 * @param {map} iter - The map whose keys are to be converted to an array
 * @return {array} The array with the map keys
 * @type {<T extends Iterator<S>>(iter: T) => S[]}
 */
export const iteratorToArray: <T extends Iterator<S, any, undefined>>(iter: T) => S[];
/**
 * L1 distance between a pair of vectors
 *
 * @description This is identical but much faster than `lDist(1)(v, w)`
 * @param {number[]} v - First vector
 * @param {number[]} w - Second vector
 * @return {number} L2 distance
 */
export function l1Dist(v: number[], w: number[]): number;
/**
 * Creates a l1 distance function tailored to the dimension of the data
 *
 * @description This is identical but faster than `l1Dist(v, w)`.
 * @param {number} dim - Dimension of the input data
 * @return {function} A function with the same signature as `l1Dist`
 */
export function l1DistByDim(dim: number): Function;
/**
 * L1 distance between a pair of points
 *
 * @description
 * Identical but much faster than `l1Dist([fromX, fromY], [toX, toY])`
 *
 * @param {number} fromX - X coordinate of the first point
 * @param {number} fromY - Y coordinate of the first point
 * @param {number} toX - X coordinate of the second point
 * @param {number} toY - Y coordinate of the first point
 * @return {number} L1 distance
 */
export function l1PointDist(fromX: number, fromY: number, toX: number, toY: number): number;
/**
 * L2 distance between a pair of vectors
 *
 * @description This is identical but much faster than `lDist(2)(v, w)`.
 * @param {number[]} v - First vector
 * @param {number[]} w - Second vector
 * @return {number} L2 distance
 */
export function l2Dist(v: number[], w: number[]): number;
/**
 * Creates a l2 distance function tailored to the dimension of the data
 *
 * @description
 * This is identical but faster than `l2Dist(v, w)`
 *
 * @param {number} dim - Dimension of the input data
 * @return {function} A function with the same signature as `l2Dist`
 */
export function l2DistByDim(dim: number): Function;
/**
 * Vector L2 norm
 *
 * @description
 * This is identical but much faster than `Math.hypot(...v)`
 *
 * @param {number[]} v - Numerical vector
 * @return {number} L2 norm
 */
export function l2Norm(v: number[]): number;
/**
 * L2 distance between a pair of points
 *
 * @description
 * Identical but much faster than `l2Dist([fromX, fromY], [toX, toY])`
 *
 * @param {number} fromX - X coordinate of the first point
 * @param {number} fromY - Y coordinate of the first point
 * @param {number} toX - X coordinate of the second point
 * @param {number} toY - Y coordinate of the first point
 * @return {number} L2 distance
 */
export function l2PointDist(fromX: number, fromY: number, toX: number, toY: number): number;
/**
 * L distance between a pair of vectors
 *
 * @param {number} l - Defines the Lp space
 * @param {number} dim - Dimension of the input data (Optional)
 * @returns {function} Returns a function to calculate the `l` distance between
 *   a pair of vectors.
 * @type {(l: number, dim: number) => (v: number[], w: number[]) => number}
 */
export const lDist: (l: number, dim: number) => (v: number[], w: number[]) => number;
/**
 * L distance between a pair of vectors
 *
 * @description
 * Identical but much faster than `lDist(l)([fromX, fromY], [toX, toY])`
 *
 * @param {number} l - Defines the Lp space
 */
export function lPointDist(l: number): (fromX: number, fromY: number, toX: number, toY: number) => any[];
/**
 * @typedef {Object} BBox Bounding box object
 * @property {number} minX Start x coordinate
 * @property {number} maxX End x coordinate
 * @property {number} minY Start y coordinate
 * @property {number} maxY End y coordinate
 */
/**
 * L distance between a pair of rectangles
 *
 * @param {number} l - Defines the Lp space
 */
export function lRectDist(l: number): (bBox1: object, bBox2: object) => number;
/**
 * Linear easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} Same as the input
 */
export function linear(t: number): number;
/**
 * Functional version of `Array.map()`
 *
 * @description
 * The pure map function is more powerful because it can be used on data types
 * other than Array too.
 *
 * @type {<I extends Iterable<T>, S>(f: (value: T) => S) => (x: I) => S[]}
 * @param {function} f - Mapping function
 * @return {array} Mapped array
 */
export const map: <I extends Iterable<T>, S>(f: (value: T) => S) => (x: I) => S[];
/**
 * Map and filter data in one iteration.
 *
 * Combining the loops is about 7-8x faster than
 *
 * @param {function} mapFn - Mapping function
 * @param {function} filterFn - Filter function
 * @return {function} A function that accepts a single array paremeter
 */
export function mapFilter(mapFn: Function, filterFn: Function): Function;
/**
 * Get the maximum number of a vector while ignoring NaNs
 *
 * @description
 * This version is muuuch faster than `Math.max(...v)` and supports vectors
 * longer than 256^2, which is a limitation of `Math.max.apply(null, v)`.
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The largest number
 */
export function max(v: number[]): number;
/**
 * Get the maximum number of a vector while ignoring NaNs
 *
 * @description
 * This version is muuuch faster than `Math.max(...v)` and supports vectors
 * longer than 256^2, which is a limitation of `Math.max.apply(null, v)`.
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The largest number
 */
export function maxNan(v: number[]): number;
/**
 * Get the max vector
 * @param {number[][]} m - Array of vectors
 * @return {number[]} Max vector
 */
export function maxVector(m: number[][]): number[];
/**
 * Get the mean of a vector
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The mean
 */
export function mean(v: number[]): number;
/**
 * Get the mean of a vector while ignoring NaNs
 *
 * @description Roughly 30% slower than `mean()`
 * @param {number[]} v - Numerical vector
 * @return {number} The mean
 */
export function meanNan(v: number[]): number;
/**
 * Get the mean vector
 * @param {number[][]} m - Array of vectors
 * @return {number[]} Mean vector
 */
export function meanVector(m: number[][]): number[];
/**
 * Get the median of a vector
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The median
 */
export function median(v: number[]): number;
/**
 * Get the median of a vector
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The median
 */
export function medianVector(v: number[]): number;
/**
 * Merge two maps
 * @type {<T1, T2, T3=T1, T4=T2>(map1: Map<T1, T2>, map2: Map<T3, T4>) => Map<T1|T3, T2|T4>}
 * @param {Map} map1 - The first map to be merged
 * @param {Map} map2 - The second map to be merged
 * @return {Map} A map with the entries of both maps
 */
export const mergeMaps: <T1, T2, T3 = T1, T4 = T2>(map1: Map<T1, T2>, map2: Map<T3, T4>) => Map<T1 | T3, T2 | T4>;
/**
 * Get the minimum number of a vector while ignoring NaNs
 *
 * @description
 * This version is muuuch faster than `Math.min(...v)` and supports vectors
 * longer than 256^2, which is a limitation of `Math.min.apply(null, v)`.
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The smallest number
 */
export function min(v: number[]): number;
/**
 * Get the minimum number of a vector while ignoring NaNs
 *
 * @description
 * This version is muuuch faster than `Math.min(...v)` and supports vectors
 * longer than 256^2, which is a limitation of `Math.min.apply(null, v)`.
 *
 * @param {number[]} v - Numerical vector
 * @return {number} The smallest number
 */
export function minNan(v: number[]): number;
/**
 * Get the min vector
 * @param {number[][]} m - Array of vectors
 * @return {number[]} Min vector
 */
export function minVector(m: number[][]): number[];
/**
 * Non-negative modulo function. E.g., `mod(-1, 5) === 4` while `-1 % 5 === -1`.
 *
 * @param {number} x - Dividend
 * @param {number} y - Divisor
 * @return {number} Remainder
 */
export function mod(x: number, y: number): number;
/**
 * Get a promise that resolves after the next `n` animation frames
 * @param {number} n - Number of animation frames to wait
 * @return {Promise} A promise that resolves after the next `n` animation frames
 */
export function nextAnimationFrame(n?: number): Promise<any>;
/**
 * An adventure into the void!
 * @return {void} The explorers find nothing but void.
 */
export function noop(): void;
/**
 * Normalize vector
 * @param {array} v - Numerical vector
 * @return {array} Unit vector
 */
export function normalize(v: any[]): any[];
/**
 * Find the nth instance of the query string
 * @param {string} str - String to search across
 * @param {string} query - String to search for
 * @param {number} n - nth instance
 * @return {number} Index of the nth query string or -1
 */
export function nthIndexOf(str: string, query: string, n?: number): number;
/**
 * Convenience function to compose functions
 * @param {...function} fns - Array of functions
 * @return {function} The composed function
 */
export function pipe(...fns: Function[]): Function;
/**
 * Quadratic in easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quadIn(t: number): number;
/**
 * Quadratic in and out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quadInOut(t: number): number;
/**
 * Quadratic out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quadOut(t: number): number;
/**
 * Quartic in easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quartIn(t: number): number;
/**
 * Quartic out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quartInOut(t: number): number;
/**
 * Quartic in and out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quartOut(t: number): number;
/**
 * Quintic in easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quintIn(t: number): number;
/**
 * Quintic out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quintInOut(t: number): number;
/**
 * Quintic in and out easing function
 * @param {number} t - The input time to be eased. Must be in [0, 1] where `0`
 *   refers to the start and `1` to the end
 * @return {number} The eased time
 */
export function quintOut(t: number): number;
/**
 * Create a random string from some alphabet
 * @param {number} length - Length of the random string
 * @param {string} alphabet - Possible characters
 * @return {string} Random string
 */
export function randomString(length: number, alphabet?: string): string;
/**
 * A function to created a range array
 * @param {number} start - Start of the range (included)
 * @param {number} end - End of the range (excluded)
 * @param {number} stepSize - Increase per step
 * @return {array} Range array
 */
export function range(start: number, end: number, stepSize?: number): any[];
/**
 * Initialize an array of a certain length using a mapping function
 *
 * @description
 * This is equivalent to `Array.from({ length }, mapFn)` but about 60% faster
 *
 * @param {number} length - Size of the array
 * @param {function} mapFn - Mapping function
 * @return {array} Initialized array
 * @type {<T = number>(length: number, mapFn: (i: number, length: number) => T) => T[]}
 */
export const rangeMap: <T = number>(length: number, mapFn: (i: number, length: number) => T) => T[];
/**
 * Remove all children of a DOM node
 * @param {object} node - DOM node whose children are to be removed
 */
export function removeAllChildren(node: object): void;
/**
 * Remove a class from an HTML or SVG element.
 * @param {HTMLElement | SVGElement} element - HTML or SVG element.
 * @param {string} className - Class name to be removed.
 */
export function removeClass(element: HTMLElement | SVGElement, className: string): void;
/**
 * Remove last child of a DOM node
 * @param {Node} node - DOM node whose last child is to be removed
 */
export function removeLastChild(node: Node): void;
/**
 * Convert RGB string to its decimal representation
 * @param {string} rgbStr - RGB string
 * @return {number} Decimal representation
 */
export function rgbStrToDec(rgbStr: string): number;
/**
 * Convert RGB(A) string to its array representation
 * @param {string} rgbStr - RGB(A) string
 * @return {number} RGB(A) array
 */
export function rgbStrToRgbArray(rgbStr: string): number;
/**
 * Convert RGB values to a HEX string
 * @param {number} r - Red component
 * @param {number} g - Green component
 * @param {number} b - Blue component
 * @return {string} HEX string
 */
export function rgbToHex(r: number, g: number, b: number): string;
/**
 * Convert RGB(A) string to its array representation
 * @param {string} rgbStr - RGB(A) string
 * @return {number} RGB(A) array
 */
export function rgbaStrToRgbaArray(rgbStr: string): number;
/**
 * Functional version of `Array.some`. More flexible and applicable to
 * other array-like data types like `NodeList`.
 *
 * @type {<I extends Iterable<T>>(f: (value: T) => Boolean) => (x: I) => Boolean}
 * @param {function} f - Function applied on every item of the array.
 * @return {Boolean} Returns `true` when `f()` evaluates to `true` on at least
 *   one element of the array-like
 */
export const some: <I extends Iterable<T>>(f: (value: T) => boolean) => (x: I) => boolean;
/**
 * Sort function for sorting in ascending order
 * @param {number} a - The numeric value of the first object to be sorted
 * @param {number} b - The numeric value of the second object to be sorted
 * @return {number} The sort value
 */
export function sortAsc(a: number, b: number): number;
/**
 * Sort function for sorting in descending order
 * @param {number} a - The numeric value of the first object to be sorted
 * @param {number} b - The numeric value of the second object to be sorted
 * @return {number} The sort value
 */
export function sortDesc(a: number, b: number): number;
/**
 * @typedef {Object} SortPosOptions Option object of `sortPos()`
 * @property {(v: any) => number} getter Value getter
 * @property {(a: number, b: number) => number} comparator Sort function
 * @property {Boolean} ignoreNull If `true` ignore `null` values
 */
/**
 * Return the sort position of each element in an array or object
 *
 * @example
 * ```js
 * let array = [9, 5, 11, -1, 0];
 * let pos = sortPos(array)
 * // >> [3, 2, 4, 0, 1]
 * // I.e., the first element of `array` is at the forth position (`3`)
 *
 * let object = { 1: 9, 2: 5, 11: 11, 100: -1, 999: 0 };
 * let pos = sortPos(object)
 * // >> { 1: 3, 2: 2, 11: 4, 100: 0, 999: 1 }
 * // I.e., the `999`th element of `object` is at the second position (`1`)
 * ```
 *
 * @param {number[] | object} source - Array or object of numerical values
 * @param {SortPosOptions} options - Option object
 * @return {array | object} Array or object of the sorted value positions
 */
export function sortPos(source: number[] | object, { getter, comparator, ignoreNull }?: SortPosOptions): any[] | object;
/**
 * Get the sum of a vector while ignoring NaNs
 *
 * @example
 * sum([0, 10, 12, 22])
 * // >> 42
 *
 * @param {number[]} values - Numerical vector
 * @return {number} The sum
 */
export function sum(values: number[]): number;
/**
 * Get the sum of a vector while ignoring NaNs
 *
 * @example
 * sum([0, 10, 12, 22])
 * // >> 42
 *
 * @param {number[]} values - Numerical vector
 * @return {number} The sum
 */
export function sumNan(values: number[]): number;
/**
 * Get the sum vector
 * @param {number[][]} m - Array of vectors
 * @return {number[]} Sum vector
 */
export function sumVector(m: number[][]): number[];
/**
 * Throttle a function
 *
 * @description
 * A throttled function will only ever be called every `wait` milliseconds at
 * most.
 *
 * @param {function} fn - Function to be throttled
 * @param {number} wait - Number of milliseconds calls are throttled
 * @return {function} Throttled function
 */
export function throttle(fn: Function, wait: number): Function;
/**
 * Throttle and debounce a function call
 *
 * Throttling a function call means that the function is called at most every
 * `interval` milliseconds no matter how frequently you trigger a call.
 * Debouncing a function call means that the function is called the earliest
 * after `finalWait` milliseconds wait time where the function was not called.
 * Combining the two ensures that the function is called at most every
 * `interval` milliseconds and is ensured to be called with the very latest
 * arguments after after `finalWait` milliseconds wait time at the end.
 *
 * The following imaginary scenario describes the behavior:
 *
 * MS | throttleTime=3 and debounceTime=3
 * 1. y(f, 3, 3)(args1) => f(args1) called
 * 2. y(f, 3, 3)(args2) => call ignored due to throttling
 * 3. y(f, 3, 3)(args3) => call ignored due to throttling
 * 4. y(f, 3, 3)(args4) => f(args4) called
 * 5. y(f, 3, 3)(args5) => all ignored due to throttling
 * 6. No call           => nothing
 * 7. No call           => f(args5) called due to debouncing
 *
 * @param {functon} fn - Function to be throttled and debounced
 * @param {number} throttleTime - Throttle intevals in milliseconds
 * @param {number} debounceTime - Debounce wait time in milliseconds. By default
 *   this is the same as `throttleTime`.
 * @return {function} - Throttled and debounced function
 */
export function throttleAndDebounce(fn: functon, throttleTime: number, debounceTime?: number): Function;
/**
 * Promise that resolves after some time
 * @param {number} msec - Time in milliseconds until the promise is resolved
 * @return {Promise} Promise resolving after `msec` milliseconds
 */
export function timeout(msec: number): Promise<any>;
/**
 * Convert a color to an RGBA color
 * @param {string | number | number[]} color - Color to be converted. Currently
 *   supports: HEX-String, HEX-Deximal, RGB, or RGBA.
 * @param {boolean} normalizeColor - If `true` the returned RGBA values will be
 *   normalized to `[0,1]`.
 * @return {number[]} Quadruple defining an RGBA color.
 */
export function toRgbaArray(color: string | number | number[], normalizeColor: boolean): number[];
/**
 * An adventure into the void!
 * @return {void} The explorers find nothing but void.
 */
export function toVoid(): void;
/**
 * Get the unique union of two vectors of integers
 * @param {number[]} v - First vector of integers
 * @param {number[]} w - Second vector of integers
 * @return {number[]} Unique union of `v` and `w`
 */
export function unionIntegers(v: number[], w: number[]): number[];
/**
 * Return unique values of an array
 * @type {<T>(a: T[], getter: (v: T) => any) => T[]}
 * @param {array} a - Input array
 * @return {array} Array with unique values
 */
export const unique: <T>(a: T[], getter: (v: T) => any) => T[];
/**
 * Update the target object by the source object. Besides extending that target
 * object, properties that are not present in the source object.
 *
 * @param {object} target - Target object or `undefined` if a new object should
 *   be created.
 * @param {object} source - Object to be cloned.
 * @return {object} Cloned `source` object
 */
export function update(target: object, source: object): object;
export var version: string;
/**
 * Promise that resolves after some time
 * @param {number} msec - Time in milliseconds until the promise is resolved
 * @return {Promise} Promise resolving after `msec` milliseconds
 */
export function wait(msec: number): Promise<any>;
/**
 * Assign a constructor to the object
 * @param {function} constructor - Constructor functions
 */
export function withConstructor(constructor: Function): (self: any) => any;
/**
 * Forward a method call
 * @param {string} name - Exposed function name
 * @param {function} fn - Function to be forwarded
 */
export function withForwardedMethod(name: string, fn: Function): (self: any) => any;
/**
 * Assign a property to an object
 * @param {string} name - Name of the property
 * @param {object} options - Option object
 * @param {*} options.initialValue - Initial value of the property
 * @param {function} options.getter - Custom getter
 * @param {function} options.setter - Custom setter
 * @param {function} options.cloner - Clone function. Used before the value
 *   is returned.
 * @param {function} options.transformer - Value transformer. Used before a new
 *   value is set.
 * @param {function} options.validator - Validator function decides whether the
 *   new and transformed value is set or not.
 */
export function withProperty(name: string, { initialValue, getter: customGetter, setter: customSetter, cloner, transformer, validator, }?: {
    initialValue: any;
    getter: Function;
    setter: Function;
    cloner: Function;
    transformer: Function;
    validator: Function;
}): (self: any) => any;
/**
 * Assign a read-only property to an object
 * @param {string} name - Name of the property
 * @param {function} getter - Getter function
 */
export function withReadOnlyProperty(name: string, getter: Function): (self: any) => any;
/**
 * Assign a static property to an object
 * @param {string} name - Name of the property
 * @param {*} value - Static value
 */
export function withStaticProperty(name: string, value: any): (self: any) => any;
